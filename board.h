Board create_board(int rows, int cols, int num_mine); //header used to call the function to create the board
void mines_around(Board* board, int i, int j); //header used to randomize the mines in the board
void check_mines_around(Board* board); //header used to check the mines around a tile on the board
